import React from "react";
import Button from "../Button";

const Percentage = () => (
  <Button color="#ff0000" label="Percentage" onClick={() => console.log("Percentage clicked")} />
);

export default Percentage;
